apiVersion: v1
kind: Service
metadata:
  name: auth-service
  labels:
    app: auth-service
    component: auth
spec:
  ports:
  - port: 80
    targetPort: 8000
    name: http
  selector:
    app: auth-service
