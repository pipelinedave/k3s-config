apiVersion: v1
kind: Service
metadata:
  name: restserver
  namespace: docspell
spec:
  ports:
  - name: http
    port: 7880
    protocol: TCP
    targetPort: 7880
  selector:
    app: restserver
  type: ClusterIP
