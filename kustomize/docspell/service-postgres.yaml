apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: docspell
spec:
  clusterIP: None
  ports:
  - name: postgres
    port: 5432
    protocol: TCP
    targetPort: postgres
  selector:
    app: postgres
  type: ClusterIP
