apiVersion: v1
kind: Service
metadata:
  name: ai-copilot-service
  labels:
    app: ai-copilot-service
    component: ai
spec:
  ports:
  - port: 80
    targetPort: 8000
    name: http
  selector:
    app: ai-copilot-service
