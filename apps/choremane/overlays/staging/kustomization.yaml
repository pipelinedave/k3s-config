apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: choremane-staging

resources:
  - ../../base
  - ../common/postgres-pvc.yaml

patches:
  - target:
      kind: Ingress
      name: frontend-ingress
    path: ingress-patch.yaml
