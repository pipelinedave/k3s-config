test/
# Dont be a doofus
*secret*
# Exclude these specific shell scripts from being ignored
!scripts/seal_secrets.sh
!scripts/seal-oauth-secret.sh
# Allow sealed secret files in git
!*-sealed.yaml
!*-secret-sealed.yaml
!*-credentials-sealed.yaml
version-configmap-dev.yaml
backup.sql
default.yaml

# Temporary files created during export
extracted/
/tmp/
*.tmp
