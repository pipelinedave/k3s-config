apiVersion: v1
kind: Service
metadata:
  name: notification-service
  labels:
    app: notification-service
    component: notifications
spec:
  ports:
  - port: 80
    targetPort: 8000
    name: http
  selector:
    app: notification-service
