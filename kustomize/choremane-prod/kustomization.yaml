apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- ../../kustomize/choremane/overlays/prod/

# Note: This namespace uses an overlay structure.
# The actual resources are defined in the base and overlays directories.
