apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: choremane-applicationset
  namespace: argocd
spec:
  generators:
    - list:
        elements:
          - name: prod
            namespace: choremane-prod
            overlay: prod
            tag: v1.0.5
          - name: staging
            namespace: choremane-staging
            overlay: staging
            tag: v1.0.4-staging
          - name: dev
            namespace: choremane-dev
            overlay: dev
            tag: HEAD
  template:
    metadata:
      name: "choremane-{{name}}"
    spec:
      project: default
      source:
        repoURL: 'https://github.com/pipelinedave/k3s-config.git'
        targetRevision: '{{tag}}'
        path: 'apps/choremane/overlays/{{overlay}}'
      destination:
        server: 'https://kubernetes.default.svc'
        namespace: '{{namespace}}'
      syncPolicy: {}
